name: Notify yowyob
on:
  push:
    branches: [HEXAGONAL]
  workflow_dispatch:
jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - env:
          GH_TOKEN: ${{ secrets.YOWYOB_SYNC_TOKEN }}
        run: gh api -X POST repos/yowyob/accounting/dispatches -f event_type=upstream-push
